# for循环
class Solution:
    def twoSum(self, nums:List[int], target: int) -> List[int]:
            for i in range(len(nums)):
		x = nums[i]
		for i in range(i+1, len(nums)):
		    y = nums[i]
		    if target == x + y
			return{i, j]
	    return[]

